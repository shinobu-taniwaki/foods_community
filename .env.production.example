# ============================================================
# 本番（オンプレ）用 環境変数テンプレート
# 設計書 §15.3 準拠。サーバー上の `.env.production` に実値を配置し、
# chmod 600 で root のみ読めるようにする。Git には絶対 commit しない。
#
# オンプレ構成: Next.js + Self-hosted Supabase を同一サーバーで稼働。
# SUPABASE_URL は Self-hosted Supabase の公開エンドポイント
# （例: https://api.example.com、または Nginx 経由の /supabase）。
# ============================================================

# --- Supabase（Self-hosted）---
# Kong（API ゲートウェイ）の公開 URL。クライアント（ブラウザ）からも到達できる必要がある。
# ★ ビルド時に焼き込まれる。本番ドメイン確定後は再ビルドが必要。
#   「一旦デプロイ」(127.0.0.1/SSHトンネル) では http://localhost:8100 を暫定使用。
NEXT_PUBLIC_SUPABASE_URL=https://api.example.com
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# service_role はサーバー専用。GitHub Actions Secrets には置かず、サーバー直接配置のみ（§15.4）
SUPABASE_SERVICE_ROLE_KEY=
# コンテナ内サーバー（RSC / Server Action / middleware）から Supabase への内部到達先。
# 未設定なら NEXT_PUBLIC_SUPABASE_URL にフォールバック（lib/env.ts getServerSupabaseUrl）。
# 同一 docker ネットワークに相乗りする構成では Kong の内部名を指す。
SUPABASE_INTERNAL_URL=http://kong:8000

# --- メール送信（Resend）---
# API キー空 = メール送信スキップ（招待はリンク手動共有にフォールバック）
# 送信ドメインの SPF/DKIM 設定（DNS）が必要。marketing-camp.jp の DNS 設定後に投入。
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@example.com
# 未設定ならアプリ名（マーケティングCampコミュニティ）を使用
RESEND_FROM_NAME=
# 返信先（サポート窓口）。未設定なら FROM を使用
RESEND_REPLY_TO=
# フッター等に表示するサポートメール。未設定なら REPLY_TO → FROM の順で使用
NEXT_PUBLIC_SUPPORT_EMAIL=

# --- β期間 ---
NEXT_PUBLIC_BETA_MODE=true
NEXT_PUBLIC_BETA_END_DATE=2026-07-31

# --- Google フォーム URL ---
NEXT_PUBLIC_FORM_PLAN_UPGRADE=https://docs.google.com/forms/.../viewform
NEXT_PUBLIC_FORM_WITHDRAWAL=https://docs.google.com/forms/.../viewform
NEXT_PUBLIC_FORM_INQUIRY=https://docs.google.com/forms/.../viewform
NEXT_PUBLIC_FORM_BUG_REPORT=https://docs.google.com/forms/.../viewform
NEXT_PUBLIC_FORM_SEMINAR=https://docs.google.com/forms/.../viewform

# --- Google フォーム prefill 用 entry ID（各フォームの「事前入力された URL を取得」から）---
NEXT_PUBLIC_FORM_PLAN_ENTRY_NAME=entry.111
NEXT_PUBLIC_FORM_PLAN_ENTRY_EMAIL=entry.222
NEXT_PUBLIC_FORM_PLAN_ENTRY_CURRENT_PLAN=entry.333
NEXT_PUBLIC_FORM_WITHDRAWAL_ENTRY_NAME=entry.111
NEXT_PUBLIC_FORM_WITHDRAWAL_ENTRY_EMAIL=entry.222
NEXT_PUBLIC_FORM_WITHDRAWAL_ENTRY_CURRENT_PLAN=entry.333
NEXT_PUBLIC_FORM_INQUIRY_ENTRY_NAME=entry.111
NEXT_PUBLIC_FORM_INQUIRY_ENTRY_EMAIL=entry.222
NEXT_PUBLIC_FORM_BUG_REPORT_ENTRY_NAME=entry.111
NEXT_PUBLIC_FORM_BUG_REPORT_ENTRY_EMAIL=entry.222
NEXT_PUBLIC_FORM_SEMINAR_ENTRY_NAME=entry.111
NEXT_PUBLIC_FORM_SEMINAR_ENTRY_EMAIL=entry.222
NEXT_PUBLIC_FORM_SEMINAR_ENTRY_CURRENT_PLAN=entry.333

# --- アクセス解析・エラー監視（DSN 空 = Sentry 無効）---
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
# ブラウザ側のエラーも拾うため NEXT_PUBLIC_ を使用（★ビルド時に焼き込み）
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
